import { PublicKey, Transaction } from '@solana/web3.js';
import { ITransfer } from '../interfaces/ITransfer';
export type TokenType = 'SOL' | 'SPL';
export declare class Disperse {
    private _config;
    private _logger;
    private constructor();
    static create(values: IDisperseConstructor): Disperse;
    generateTransactions(): Promise<Transaction[]>;
}
export interface IDisperseConstructor {
    tokenType: TokenType;
    mint?: PublicKey;
    maximumAmount?: number;
    transfers?: ITransfer[];
    recipients?: PublicKey[];
    fixedAmount?: number;
    sender: PublicKey;
}
